name: Call Reusable Workflow

on:
  workflow_dispatch:

jobs:
  call-reusable:
    uses: bhushan-s-pathak/azure-gitops/.github/workflows/reusable.yml@main
    with:
      message: "Hello from parent"
    secrets:
      MY_SECRET: ${{ secrets.MY_SECRET }}
